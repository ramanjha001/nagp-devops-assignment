

server:
  port: 8085

spring:
  application:
    name: userservice-test
  datasource:
    url: jdbc:mysql://localhost:3306/usernagpdemodb
    username: root
    password: ramanjha
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect